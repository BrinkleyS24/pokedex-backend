<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs') %>
</head>

<body>
    <h1>Pokedex</h1>
    <div class="container">
        <ul>
            <% data.forEach((pokemon, i)=> { %>
                <li>
                    <a href="/pokemon/<%= i %>">
                        <img src="<%= pokemon.img %> " alt="<%= pokemon.name %> "> 
                    </a>
                </li>
                <% }) %>
        </ul>
    </div>
</body>

</html>